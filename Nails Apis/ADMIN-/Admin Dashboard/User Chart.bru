meta {
  name: User Chart
  type: http
  seq: 3
}

get {
  url: {{base_url}}/v1/admin/chart/users?filter=weekly
  body: multipartForm
  auth: bearer
}

params:query {
  filter: weekly
  ~filter: monthly
}

auth:bearer {
  token: {{super_adminToken}}
}

body:multipart-form {
  question: {{$randomLoremLines}}
  answer: {{$randomLoremSentence}}
  is_active: 1
}

settings {
  encodeUrl: true
}
