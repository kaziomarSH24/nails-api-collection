meta {
  name: delete todo
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/v1/todos/1
  body: none
  auth: bearer
}

params:query {
  ~per_page: 1
}

auth:bearer {
  token: {{userToken}}
}

settings {
  encodeUrl: true
}
