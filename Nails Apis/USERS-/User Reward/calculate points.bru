meta {
  name: calculate points
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/user/rewards/redeem
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

body:multipart-form {
  points_to_redeem: 2
  cart_total: 85
}

settings {
  encodeUrl: true
}
