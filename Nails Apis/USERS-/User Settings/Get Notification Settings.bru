meta {
  name: Get Notification Settings
  type: http
  seq: 1
}

get {
  url: {{base_url}}/v1/settings/notifications
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

settings {
  encodeUrl: true
}
